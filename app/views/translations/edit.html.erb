<%= stylesheet_link_tag 'actiontext' %>
<%= javascript_pack_tag 'translations/new' %>
<%# TODO: use the real ID when implementing the edit and update actions %>
<%# are you just not suppose to nest update routes??? %>
<%= form_for @translation, method: :PUT, url: section_translations_path(section_id: @section.id) + "/1" do |f| %>
  <div class="field">
    <%= f.rich_text_area :content %>
  </div>
  <%= f.submit %>
<% end %>
